fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
beautifulsoup4==4.12.3
lxml==5.3.0
pydantic==2.9.2

# Optional – nur für leistungsfähigeres Scraping JS-lastiger Shops.
# Lokal aktivieren mit:  pip install playwright && playwright install chromium
# Das Tool funktioniert auch ohne Playwright (httpx-Fallback + manueller Modus).
# playwright==1.47.0
